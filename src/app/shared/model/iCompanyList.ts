/**
 * this interface has the properties for placed students list that will appear ince user selects college and then company
 */
export interface ICompany{
    id? : string;
    studentName: string;
    branchAndYear: string;
}