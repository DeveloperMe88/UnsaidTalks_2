/**
 * this interface has properties for blog box that appears when user click on blog option in navigation bar.
 */
export interface BBox{
    id? : string;
    title: string;
    imageUrl: string;
}